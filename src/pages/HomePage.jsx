import React from 'react';
import MenuBar from '../components/MenuBar';

const HomePage = () => {
    return (
        <MenuBar>
            This is homepage
        </MenuBar>
    );
};

export default HomePage;